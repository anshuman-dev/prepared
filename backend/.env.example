# Server Configuration
PORT=8080
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

# Google Cloud Platform (for Firestore)
GOOGLE_PROJECT_ID=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# Firestore
FIRESTORE_DATABASE_ID=(default)

# JWT Authentication (not actively used - Firebase Auth handles tokens)
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRATION=7d

# ElevenLabs Voice Agent
ELEVENLABS_API_KEY=your-elevenlabs-api-key
ELEVENLABS_AGENT_ID=your-elevenlabs-agent-id
# For local dev use localhost, for production use ngrok or deployed URL
BACKEND_URL=http://localhost:8080

# Gemini API (Free Tier)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
# Available models: gemini-flash-latest, gemini-1.5-flash, gemini-2.0-flash
GEMINI_MODEL=gemini-flash-latest
